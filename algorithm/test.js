let fs = require("fs");
let input = fs.readFileSync("./input.txt").toString().trim().split("\n");

let arr = [];
let obj = {};

for (let i = 0; i < input.length; i++) {
  if (input[i][2] !== "P") {
  }
}
